---
phase: 03-date-bounded-series
plan: 03
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-02
files_modified:
  - packages/uk-data/src/uk_data/cli.py
  - packages/uk-data/tests/test_client_integration.py
  - packages/uk-data/tests/test_client.py
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - TS-03
  - TS-02
must_haves:
  truths:
    - "Legacy limit-based calls remain functional with defined migration behavior."
    - "Date-range semantics are documented at user-facing entry points."
  artifacts:
    - path: "packages/uk-data/src/uk_data/cli.py"
      provides: "CLI accepts and documents start/end date params"
      contains: "--start-date"
    - path: "packages/uk-data/tests/test_client.py"
      provides: "Compatibility coverage for limit-only paths"
      contains: "limit"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 3 plan list and documented objective status"
      contains: "03-03-PLAN.md"
  key_links:
    - from: "packages/uk-data/src/uk_data/cli.py"
      to: "packages/uk-data/src/uk_data/client.py"
      via: "CLI get-series forwards start/end dates"
      pattern: "client.get_series"
---

<objective>
Finalize migration/deprecation path and user-facing documentation for date-bounded series.

Purpose: Complete TS-03 by making compatibility behavior explicit (limit-only still valid, date window preferred) and test-guarding the migration contract.
Output: Updated CLI interface/docs, compatibility tests, and roadmap plan metadata updates.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-date-bounded-series/03-RESEARCH.md
@.planning/phases/03-date-bounded-series/03-01-SUMMARY.md
@.planning/phases/03-date-bounded-series/03-02-SUMMARY.md
@packages/uk-data/src/uk_data/cli.py
@packages/uk-data/tests/test_client.py
@packages/uk-data/tests/test_client_integration.py

<interfaces>
From `packages/uk-data/src/uk_data/cli.py`:

```python
def get_series_cmd(
    concept: str,
    source: str | None = None,
    limit: int | None = None,
    data_path: Path | None = None,
    output: str | None = None,
) -> None:
    ts = client.get_series(concept, source=source, limit=limit if limit is not None else 20, **series_kwargs)
```

Migration contract from research:

- limit-only => unchanged behavior
- window-only => all points in inclusive window
- window + limit => apply window first, then limit
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add CLI migration surface for explicit date windows (TS-03)</name>
  <files>packages/uk-data/src/uk_data/cli.py, packages/uk-data/tests/test_client.py</files>
  <read_first>packages/uk-data/src/uk_data/cli.py, packages/uk-data/src/uk_data/client.py, packages/uk-data/tests/test_client.py</read_first>
  <behavior>
    - Test 1: CLI `get-series` accepts `--start-date` and `--end-date` and forwards to `client.get_series`.
    - Test 2: Existing CLI/client limit-only behavior remains unchanged when date args are absent.
  </behavior>
  <action>Extend `get_series_cmd` with `--start-date` and `--end-date` options (string ISO input) and pass them to `client.get_series` only when provided. Update help text to document migration semantics: `limit` remains supported for backward compatibility; explicit date windows are the preferred bounded API. Preserve default limit behavior when no date window is passed.</action>
  <acceptance_criteria>- `cli.py` contains `--start-date` and `--end-date` Typer options.
- CLI help/docstring text includes explicit migration semantics for `limit` vs date window.
- Client tests still pass for limit-only paths after CLI changes.</acceptance_criteria>
  <verify>
    <automated>uv run pytest packages/uk-data/tests/test_client.py -q</automated>
  </verify>
  <done>CLI exposes explicit date-window parameters while keeping compatibility behavior stable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock migration behavior with integration-oriented compatibility tests</name>
  <files>packages/uk-data/tests/test_client_integration.py, packages/uk-data/tests/test_client.py</files>
  <read_first>packages/uk-data/tests/test_client_integration.py, packages/uk-data/tests/test_client.py, packages/uk-data/src/uk_data/client.py</read_first>
  <behavior>
    - Test 1: limit-only integration/unit paths continue to return series successfully.
    - Test 2: date-window path is accepted and returns bounded data when network/source is available.
    - Test 3: dual-parameter path (`start_date` + `end_date` + `limit`) is exercised to enforce documented precedence.
  </behavior>
  <action>Add/update tests so TS-03 migration contract is explicit and regression-resistant: preserve existing limit-only assertions, add date-window acceptance assertions, and add combined window+limit checks where deterministic fixtures/mocks exist. Keep network-sensitive integration tests behind existing reachability skips/markers.</action>
  <acceptance_criteria>- `test_client.py` or `test_client_integration.py` includes migration-path assertions for limit-only and date-window usage.
- Combined window+limit behavior is covered in at least one deterministic test.
- Tests remain marker-compliant (`integration` where required).</acceptance_criteria>
  <verify>
    <automated>uv run pytest packages/uk-data/tests/test_client.py packages/uk-data/tests/test_client_integration.py -q -m "not integration"</automated>
  </verify>
  <done>Migration behavior is test-locked and safe for downstream callers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI arguments → client API | User-provided date strings and limit values enter core fetch layer |
| Integration tests → live external APIs | Non-deterministic upstream payloads can change over time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | T (Tampering) | CLI input parsing | mitigate | Constrain CLI date inputs to explicit options and rely on downstream validation from shared date parser |
| T-03-08 | R (Repudiation) | Migration semantics drift | mitigate | Encode precedence rules (window then limit) in unit/integration tests and CLI docs |
| T-03-09 | D (Denial of Service) | integration test runtime | accept | Existing `integration` markers and reachability guards already prevent blocking default test suite |
</threat_model>

<verification>
- `uv run pytest packages/uk-data/tests/test_client.py -q`
- `uv run pytest packages/uk-data/tests/test_client_integration.py -q -m "not integration"`
- `uv run ruff check packages/uk-data/src/uk_data/cli.py packages/uk-data/tests/test_client.py packages/uk-data/tests/test_client_integration.py`
</verification>

<success_criteria>
- TS-03 migration behavior is documented at CLI/client entry points.
- Legacy `limit` paths remain working and test-covered.
- Combined date-window + limit behavior is explicitly validated.
</success_criteria>

<output>
After completion, create `.planning/phases/03-date-bounded-series/03-03-SUMMARY.md`
</output>
