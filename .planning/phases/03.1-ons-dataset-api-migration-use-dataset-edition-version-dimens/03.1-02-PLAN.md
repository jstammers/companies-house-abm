---
phase: 03.1-ons-dataset-api-migration-use-dataset-edition-version-dimens
plan: 02
type: execute
wave: 2
depends_on: [03.1-01]
files_modified:
  - packages/uk-data/src/uk_data/adapters/ons.py
  - packages/uk-data/tests/adapters/test_ons.py
  - packages/uk-data/tests/adapters/test_ons_integration.py
autonomous: true
requirements: [TBD]
must_haves:
  truths:
    - "ONSAdapter manifest-backed series use the dataset observation provider path transparently."
    - "Date-window and limit semantics from Phase 03 remain unchanged after transport migration."
    - "Fallback series continue to resolve via point-series handlers without regression."
  artifacts:
    - path: "packages/uk-data/src/uk_data/adapters/ons.py"
      provides: "Adapter transport wiring and canonical TimeSeries mapping"
      contains: "class ONSAdapter"
    - path: "packages/uk-data/tests/adapters/test_ons.py"
      provides: "Offline ONS adapter migration regressions"
    - path: "packages/uk-data/tests/adapters/test_ons_integration.py"
      provides: "Live endpoint compatibility checks for advertised series"
  key_links:
    - from: "packages/uk-data/src/uk_data/adapters/ons.py"
      to: "packages/uk-data/src/uk_data/adapters/ons_provider.py"
      via: "fetch_sdmx_series() call path"
      pattern: "fetch_sdmx_series\("
    - from: "packages/uk-data/src/uk_data/adapters/ons.py"
      to: "packages/uk-data/src/uk_data/utils/timeseries.py"
      via: "filter_observations_by_date_window"
      pattern: "filter_observations_by_date_window"
---

<objective>
Wire ONS adapter behavior to the migrated provider contract and lock migration semantics with adapter-level tests.

Purpose: Complete endpoint migration at the adapter boundary without changing caller-facing series behavior.
Output: Updated adapter transport usage and full regression coverage (offline + integration).
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-date-bounded-series/03-01-SUMMARY.md
@.planning/phases/03-date-bounded-series/03-02-SUMMARY.md
@.planning/phases/03.1-ons-dataset-api-migration-use-dataset-edition-version-dimens/03.1-RESEARCH.md
@.planning/phases/03.1-ons-dataset-api-migration-use-dataset-edition-version-dimens/03.1-01-PLAN.md
@packages/uk-data/src/uk_data/adapters/ons.py
@packages/uk-data/tests/adapters/test_ons.py
@packages/uk-data/tests/adapters/test_ons_integration.py

<interfaces>
From `packages/uk-data/src/uk_data/adapters/ons.py`:

```python
class ONSAdapter:
    def available_series(self) -> list[str]: ...
    def fetch_series(self, series_id: str, **kwargs: object): ...
```

From `packages/uk-data/src/uk_data/adapters/ons_provider.py`:

```python
def fetch_sdmx_series(
    entry: ONSSeriesManifestEntry,
    *,
    limit: int = 20,
    start_date: str | None = None,
    end_date: str | None = None,
) -> list[dict[str, str]]
```

From `packages/uk-data/src/uk_data/utils/timeseries.py`:

```python
def filter_observations_by_date_window(
    observations: list[dict[str, object]],
    *,
    start_date: str | date | datetime | None = None,
    end_date: str | date | datetime | None = None,
) -> list[dict[str, object]]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate adapter transport path while preserving caller semantics</name>
  <files>packages/uk-data/src/uk_data/adapters/ons.py, packages/uk-data/tests/adapters/test_ons.py</files>
  <read_first>packages/uk-data/src/uk_data/adapters/ons.py, packages/uk-data/tests/adapters/test_ons.py, .planning/phases/03-date-bounded-series/03-02-SUMMARY.md</read_first>
  <behavior>
    - Test 1: Manifest-backed series still return canonical `TimeSeries` values after provider path migration.
    - Test 2: Inclusive date-window + filter-before-limit behavior remains unchanged.
    - Test 3: `HP7A` and `D7RA` still route through fallback point-series handlers.
  </behavior>
  <action>Refactor ONS adapter transport internals to rely exclusively on provider-level dataset observation retrieval for `transport="sdmx"` entries and remove unused legacy retrieval helpers from the manifest-backed path. Preserve existing public function signatures and time-window/limit ordering from Phase 03. Update `test_ons.py` assertions to explicitly cover migrated transport behavior and prevent regressions back to legacy retrieval shortcuts.</action>
  <acceptance_criteria>
    - `packages/uk-data/src/uk_data/adapters/ons.py` no longer uses legacy manifest-backed retrieval helper branches for SDMX series.
    - `packages/uk-data/tests/adapters/test_ons.py` contains regression tests for window-first-then-limit semantics and fallback handlers.
    - Adapter unit tests pass with the migrated provider path.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest packages/uk-data/tests/adapters/test_ons.py -q</automated>
  </verify>
  <done>ONS adapter behavior is transport-migrated yet caller-compatible for all advertised series IDs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock live compatibility across all advertised ONS series</name>
  <files>packages/uk-data/tests/adapters/test_ons_integration.py</files>
  <read_first>packages/uk-data/tests/adapters/test_ons_integration.py, packages/uk-data/src/uk_data/adapters/ons.py, packages/uk-data/src/uk_data/adapters/ons_manifest.py</read_first>
  <behavior>
    - Test 1: Integration loop over `available_series()` still succeeds/skips deterministically under network gating.
    - Test 2: Live fetches validate source tagging and non-null latest values for manifest-backed series.
  </behavior>
  <action>Update integration tests only where needed to align with dataset endpoint migration assumptions (without hardcoding deprecated retrieval paths). Preserve existing network reachability guard and cache clearing behavior. Ensure integration assertions remain transport-agnostic at adapter boundary (i.e., they validate data availability/shape, not internal call mechanics).</action>
  <acceptance_criteria>
    - Integration test module continues to gate on endpoint reachability before live calls.
    - Integration tests iterate through `adapter.available_series()` and validate non-null results/source tagging.
    - `pytest -m integration` target remains runnable without test rewrites outside ONS integration module.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest packages/uk-data/tests/adapters/test_ons_integration.py -m integration -q</automated>
  </verify>
  <done>Live compatibility checks protect the migrated adapter against endpoint-level regressions while preserving current integration test ergonomics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Adapter → Provider | Adapter relies on provider-normalized external observations |
| Integration tests → Live ONS API | Network response availability and schema are untrusted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.1-05 | T | `ONSAdapter.fetch_series` transport branch | mitigate | Restrict manifest-backed path to provider function and raise explicit errors for unsupported series ids. |
| T-03.1-06 | I | Date-window filtering in adapter | mitigate | Preserve shared utility filtering and lock with regression tests for inclusive bounds + ordering. |
| T-03.1-07 | D | Live integration endpoint volatility | accept | Keep network-gated integration tests with skip-on-unreachable behavior. |
| T-03.1-08 | R | Migration ambiguity in code paths | mitigate | Remove legacy-path branches from SDMX flow and assert migrated behavior in adapter tests. |
</threat_model>

<verification>
- `uv run pytest packages/uk-data/tests/adapters/test_ons.py -q`
- `uv run pytest packages/uk-data/tests/adapters/test_ons_provider.py -q`
- `uv run pytest packages/uk-data/tests/adapters/test_ons_integration.py -m integration -q`
</verification>

<success_criteria>
- ONS adapter manifest-backed retrieval path is fully migrated to dataset endpoints through provider abstraction.
- Date-window semantics from Phase 03 remain intact.
- Offline and integration tests both protect migrated behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-ons-dataset-api-migration-use-dataset-edition-version-dimens/03.1-02-SUMMARY.md`
</output>
