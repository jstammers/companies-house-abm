---
phase: 03.1-ons-dataset-api-migration-use-dataset-edition-version-dimens
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/uk-data/src/uk_data/adapters/ons.py
  - packages/uk-data/src/uk_data/adapters/ons_models.py
  - packages/uk-data/tests/adapters/test_ons.py
  - packages/uk-data/tests/adapters/test_ons_dataset_api.py
  - packages/uk-data/src/uk_data/adapters/ons_manifest.py
  - packages/uk-data/src/uk_data/adapters/ons_provider.py
autonomous: true
requirements: [TBD]
must_haves:
  truths:
    - "ONSAdapter exposes dataset-first API methods backed by official ONS dataset endpoints."
    - "Dataset metadata and observation payloads are represented with explicit Pydantic models."
    - "list_datasets uses caching to avoid repeated API calls."
    - "ons_manifest and ons_provider are removed from the runtime path."
  artifacts:
    - path: "packages/uk-data/src/uk_data/adapters/ons_models.py"
      provides: "Pydantic contracts for dataset/version/observation payloads"
      exports: ["ONSDatasetInfo", "ONSDatasetVersionInfo", "ONSObservation"]
    - path: "packages/uk-data/src/uk_data/adapters/ons.py"
      provides: "Dataset API methods and cached dataset catalog operations"
      contains: ["ONSAdapter.list_datasets", "ONSAdapter.get_dataset", "ONSAdapter.get_version", "ONSAdapter.get_observation", "ONSAdapter.get_observation_series"]
    - path: "packages/uk-data/tests/adapters/test_ons_dataset_api.py"
      provides: "Offline contract tests for dataset API methods and caching behavior"
  key_links:
    - from: "packages/uk-data/src/uk_data/adapters/ons.py"
      to: "https://api.beta.ons.gov.uk/v1/datasets"
      via: "ONSAdapter.list_datasets()"
      pattern: "GET /datasets"
    - from: "packages/uk-data/src/uk_data/adapters/ons.py"
      to: "https://api.beta.ons.gov.uk/v1/datasets/{id}/editions/{edition}/versions/{version}/observations"
      via: "ONSAdapter.get_observation / get_observation_series"
      pattern: "observations"
---

<objective>
Implement a dataset-first ONS adapter API and remove obsolete ONS transport layers.

Purpose: Replace manifest/provider indirection with direct, typed ONS dataset endpoint methods that become the source of truth for series retrieval.
Output: New adapter methods + Pydantic models + caching + deletion of obsolete modules.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.1-ons-dataset-api-migration-use-dataset-edition-version-dimens/03.1-RESEARCH.md
@packages/uk-data/src/uk_data/adapters/ons.py
@packages/uk-data/tests/adapters/test_ons.py
@packages/uk-data/tests/adapters/test_ons_integration.py

<interfaces>
Add to `packages/uk-data/src/uk_data/adapters/ons.py`:

```python
def list_datasets(self, *, limit: int = 20, offset: int = 0, dataset_type: str | None = None) -> list[ONSDatasetInfo]
def get_dataset(self, dataset_id: str) -> ONSDatasetInfo
def get_version(self, dataset_id: str, edition: str, version: str | int) -> ONSDatasetVersionInfo
def get_observation(self, dataset_id: str, edition: str, version: str | int, **dimensions: str) -> ONSObservation
def get_observation_series(self, dataset_id: str, edition: str, version: str | int, **dimensions: str) -> list[ONSObservation]
```

Add models in `packages/uk-data/src/uk_data/adapters/ons_models.py`:

```python
class ONSDatasetInfo(BaseModel): ...
class ONSDatasetVersionInfo(BaseModel): ...
class ONSObservation(BaseModel): ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Introduce typed ONS dataset models and adapter dataset API methods</name>
  <files>packages/uk-data/src/uk_data/adapters/ons.py, packages/uk-data/src/uk_data/adapters/ons_models.py, packages/uk-data/tests/adapters/test_ons_dataset_api.py</files>
  <read_first>packages/uk-data/src/uk_data/adapters/ons.py, .planning/phases/03.1-ons-dataset-api-migration-use-dataset-edition-version-dimens/03.1-RESEARCH.md</read_first>
  <behavior>
    - Test 1: `list_datasets()` returns `list[ONSDatasetInfo]` and caches responses for identical query args.
    - Test 2: `get_dataset(dataset_id)` returns one `ONSDatasetInfo` using `/datasets/{id}`.
    - Test 3: `get_version(dataset_id, edition, version)` returns `ONSDatasetVersionInfo` using `/datasets/{id}/editions/{edition}/versions/{version}`.
    - Test 4: `get_observation(...)` returns one `ONSObservation` from `/observations` with concrete dimension filters.
    - Test 5: `get_observation_series(...)` returns `list[ONSObservation]` for wildcard time/dimension queries.
  </behavior>
  <action>Create `ons_models.py` with Pydantic models for dataset, version, and observation payloads based on the ONS docs/examples. Implement new `ONSAdapter` methods for datasets/version/observation retrieval via official ONS endpoints. Add adapter-local caching for `list_datasets` (argument-keyed) to avoid repeated catalog calls. Implement robust response normalization that tolerates missing `@context` and shape variability noted by ONS.</action>
  <acceptance_criteria>
    - `packages/uk-data/src/uk_data/adapters/ons_models.py` exists and exports all three required models.
    - `ONSAdapter` includes `list_datasets`, `get_dataset`, `get_version`, `get_observation`, `get_observation_series`.
    - `list_datasets` performs cache-hit behavior on repeated identical calls.
    - New offline tests validate endpoint paths, query forwarding, and model parsing.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest packages/uk-data/tests/adapters/test_ons_dataset_api.py -q</automated>
  </verify>
  <done>Dataset-first adapter API exists, typed payloads are validated, and repeated catalog calls are cached.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Remove obsolete ons_manifest/ons_provider modules and migrate tests</name>
  <files>packages/uk-data/src/uk_data/adapters/ons_manifest.py, packages/uk-data/src/uk_data/adapters/ons_provider.py, packages/uk-data/tests/adapters/test_ons_provider.py, packages/uk-data/tests/adapters/test_ons.py</files>
  <read_first>packages/uk-data/src/uk_data/adapters/ons.py, packages/uk-data/tests/adapters/test_ons.py</read_first>
  <behavior>
    - Test 1: Runtime imports no longer reference `ons_manifest` or `ons_provider`.
    - Test 2: Existing ONS adapter tests remain green after migration to dataset API methods.
    - Test 3: No behavioral regression in fallback series paths.
  </behavior>
  <action>Delete `ons_manifest.py`, `ons_provider.py`, and provider-specific tests after rewiring adapter internals. Update remaining adapter tests to validate behavior through `ONSAdapter` public methods only. Ensure no stale imports remain in codebase and update any package exports as needed.</action>
  <acceptance_criteria>
    - `packages/uk-data/src/uk_data/adapters/ons_manifest.py` and `packages/uk-data/src/uk_data/adapters/ons_provider.py` are removed.
    - `grep` over `packages/uk-data/src` finds no references to those modules.
    - ONS adapter test suite passes with dataset-first methods in place.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest packages/uk-data/tests/adapters/test_ons.py -q</automated>
    <automated>uv run pytest packages/uk-data/tests/adapters/test_ons_dataset_api.py -q</automated>
  </verify>
  <done>Obsolete transport layers are removed; adapter uses only dataset-first method surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ONSAdapter → ONS API | Untrusted dataset/version/observation payloads enter model parsing path |
| Cache layer → callers | Cached catalog data can become stale relative to live API |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.1-01 | T | ONS dataset/version model parsing | mitigate | Validate payloads via strict Pydantic models and fail with explicit errors on malformed responses. |
| T-03.1-02 | I | Observation query parameter construction | mitigate | Add tests asserting exact forwarded dimension/filter query keys. |
| T-03.1-03 | D | ONS catalog endpoint availability | accept | Keep offline tests and allow integration skips when API is unreachable. |
| T-03.1-04 | S | list_datasets cache staleness | mitigate | Add clear-cache hook and short-lived/process-local cache design; document refresh behavior. |
</threat_model>

<verification>
Run adapter API unit tests and ensure obsolete modules are removed safely:

- `uv run pytest packages/uk-data/tests/adapters/test_ons_dataset_api.py -q`
- `uv run pytest packages/uk-data/tests/adapters/test_ons.py -q`
</verification>

<success_criteria>
- ONSAdapter exposes dataset-first methods backed by official ONS endpoints.
- `ons_manifest` and `ons_provider` are removed from the codebase.
- Unit tests prove caching, endpoint path/query construction, and typed response parsing.
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-ons-dataset-api-migration-use-dataset-edition-version-dimens/03.1-01-SUMMARY.md`
</output>
