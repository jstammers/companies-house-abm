---
phase: 03.1-ons-dataset-api-migration-use-dataset-edition-version-dimens
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/uk-data/src/uk_data/adapters/ons_manifest.py
  - packages/uk-data/src/uk_data/adapters/ons_provider.py
  - packages/uk-data/tests/adapters/test_ons_provider.py
autonomous: true
requirements: [TBD]
must_haves:
  truths:
    - "Manifest-backed ONS series are fetched through dataset/edition/version observation endpoints."
    - "Provider resolves dimensions and observation payloads without relying on legacy retrieval paths."
    - "Date bounds are forwarded into endpoint-level observation queries."
  artifacts:
    - path: "packages/uk-data/src/uk_data/adapters/ons_manifest.py"
      provides: "Dataset endpoint contract metadata per ONS series"
      contains: "ONSSeriesManifestEntry"
    - path: "packages/uk-data/src/uk_data/adapters/ons_provider.py"
      provides: "Dataset endpoint observation retrieval and normalization"
      exports: ["fetch_sdmx_series"]
    - path: "packages/uk-data/tests/adapters/test_ons_provider.py"
      provides: "Provider endpoint contract and normalization regression coverage"
  key_links:
    - from: "packages/uk-data/src/uk_data/adapters/ons_manifest.py"
      to: "packages/uk-data/src/uk_data/adapters/ons_provider.py"
      via: "dataset/edition/version metadata lookup"
      pattern: "dataset_id|edition|version"
    - from: "packages/uk-data/src/uk_data/adapters/ons_provider.py"
      to: "ONS dataset observations endpoint"
      via: "HTTP GET to /datasets/{dataset}/editions/{edition}/versions/{version}/observations"
      pattern: "observations"
---

<objective>
Define and implement the ONS provider transport contract for dataset/edition/version/dimension/observation retrieval so adapter-level callers no longer depend on legacy retrieval paths.

Purpose: Establish a deterministic, testable endpoint contract for ONS observation fetching in preparation for adapter migration.
Output: Updated manifest/provider modules plus provider contract tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.1-ons-dataset-api-migration-use-dataset-edition-version-dimens/03.1-RESEARCH.md
@packages/uk-data/src/uk_data/adapters/ons_manifest.py
@packages/uk-data/src/uk_data/adapters/ons_provider.py
@packages/uk-data/tests/adapters/test_ons_provider.py

<interfaces>
From `packages/uk-data/src/uk_data/adapters/ons_manifest.py`:

```python
@dataclass(frozen=True)
class ONSSeriesManifestEntry:
    source_series_id: str
    concept: str
    name: str
    frequency: str
    units: str
    seasonal_adjustment: str
    geography: str = "UK"
    transport: Transport = "sdmx"
    provider_id: str = "ONS"
    dataset_id: str | None = None
    sdmx_key: str | None = None
    fallback_handler: str | None = None
```

From `packages/uk-data/src/uk_data/adapters/ons_provider.py`:

```python
def fetch_sdmx_series(
    entry: ONSSeriesManifestEntry,
    *,
    limit: int = 20,
    start_date: str | None = None,
    end_date: str | None = None,
) -> list[dict[str, str]]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend ONS manifest to carry dataset endpoint contract metadata</name>
  <files>packages/uk-data/src/uk_data/adapters/ons_manifest.py, packages/uk-data/tests/adapters/test_ons_provider.py</files>
  <read_first>packages/uk-data/src/uk_data/adapters/ons_manifest.py, packages/uk-data/tests/adapters/test_ons_provider.py, .planning/phases/03.1-ons-dataset-api-migration-use-dataset-edition-version-dimens/03.1-RESEARCH.md</read_first>
  <behavior>
    - Test 1: Each manifest-backed ONS series entry exposes non-null dataset endpoint metadata required for observations lookup.
    - Test 2: Manifest metadata for ABMI and MGSX continues to map to current datasets while adding edition/version metadata fields.
  </behavior>
  <action>Update `ONSSeriesManifestEntry` to include explicit dataset endpoint fields (`edition_id`, `version_ref`, and optional `dimensions` mapping) and populate manifest-backed entries (`ABMI`, `RPHQ`, `NRJS`, `MGSX`, `KAB9`) with concrete values. Keep fallback entries (`HP7A`, `D7RA`) as `transport="fallback"` with null endpoint metadata so they remain outside the observations path. Add/adjust provider tests to assert these fields are present and validated for manifest-backed entries.</action>
  <acceptance_criteria>
    - `packages/uk-data/src/uk_data/adapters/ons_manifest.py` defines `edition_id` and `version_ref` on `ONSSeriesManifestEntry`.
    - `ONS_SERIES_MANIFEST["ABMI"]` and `ONS_SERIES_MANIFEST["MGSX"]` include non-null `dataset_id`, `edition_id`, and `version_ref`.
    - `packages/uk-data/tests/adapters/test_ons_provider.py` contains assertions covering the new manifest endpoint fields.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest packages/uk-data/tests/adapters/test_ons_provider.py -q</automated>
  </verify>
  <done>Manifest entries provide endpoint metadata required to construct dataset observation requests without relying on legacy retrieval inference.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement provider retrieval using dataset/edition/version observation endpoints</name>
  <files>packages/uk-data/src/uk_data/adapters/ons_provider.py, packages/uk-data/tests/adapters/test_ons_provider.py</files>
  <read_first>packages/uk-data/src/uk_data/adapters/ons_provider.py, packages/uk-data/tests/adapters/test_ons_provider.py, packages/uk-data/src/uk_data/_http.py</read_first>
  <behavior>
    - Test 1: Provider composes endpoint path `/datasets/{dataset}/editions/{edition}/versions/{version}/observations` with configured dimensions.
    - Test 2: `start_date` and `end_date` are passed as observation query filters.
    - Test 3: Observation payload is normalized into chronological `{date, value}` rows and still respects `limit` tail slicing.
  </behavior>
  <action>Refactor `fetch_sdmx_series` implementation to call ONS dataset observation endpoints directly (using existing `uk_data._http` helpers) rather than `pandasdmx.Request(...).data(...)` legacy retrieval flow. Keep exported function name stable to avoid adapter churn, but implement endpoint composition and payload normalization in-provider. Preserve `ModuleNotFoundError` behavior only where still applicable; otherwise surface clear `ValueError` for malformed endpoint metadata. Update provider tests to assert exact endpoint path composition and query parameter forwarding.</action>
  <acceptance_criteria>
    - `packages/uk-data/src/uk_data/adapters/ons_provider.py` contains endpoint path logic including `/observations`.
    - Provider tests assert forwarding of `start_date` and `end_date` query filters.
    - Provider tests assert normalized output row shape remains `[{"date": ..., "value": ...}]`.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest packages/uk-data/tests/adapters/test_ons_provider.py -q</automated>
  </verify>
  <done>Provider fetch path is dataset-endpoint based, deterministic, and protected by offline tests for path/query/normalization behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Provider → ONS API | Untrusted external observation payloads enter normalization path |
| Manifest → Provider | Static metadata controls external request routing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.1-01 | T | `ons_manifest.py` endpoint metadata | mitigate | Validate required dataset endpoint fields for `transport=sdmx`; raise `ValueError` on missing metadata before HTTP request. |
| T-03.1-02 | I | `ons_provider.py` observation payload parsing | mitigate | Normalize only expected observation fields and ignore malformed entries lacking date/value. |
| T-03.1-03 | D | ONS network dependency | accept | Keep existing offline unit tests and allow integration tests to skip when endpoint unreachable. |
| T-03.1-04 | E | Query parameter forwarding (`start_date`, `end_date`) | mitigate | Add tests asserting exact forwarded query keys to prevent unintended parameter injection. |
</threat_model>

<verification>
Run provider unit test suite and ensure no regressions in endpoint contract assertions:

- `uv run pytest packages/uk-data/tests/adapters/test_ons_provider.py -q`
</verification>

<success_criteria>
- Provider no longer relies on legacy retrieval composition for manifest-backed series.
- Endpoint metadata required for dataset observations is explicit in manifest entries.
- Unit tests prove endpoint path/query construction and normalized observation output.
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-ons-dataset-api-migration-use-dataset-edition-version-dimens/03.1-01-SUMMARY.md`
</output>
