---
phase: 03-date-bounded-series
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - packages/uk-data/src/uk_data/adapters/ons.py
  - packages/uk-data/src/uk_data/adapters/boe.py
  - packages/uk-data/src/uk_data/adapters/land_registry.py
  - packages/uk-data/tests/adapters/test_ons.py
  - packages/uk-data/tests/adapters/test_boe.py
  - packages/uk-data/tests/adapters/test_land_registry.py
autonomous: true
requirements:
  - TS-01
  - TS-02
must_haves:
  truths:
    - "Adapters honor start_date/end_date consistently for time-series responses."
    - "When both date window and limit are provided, windowing occurs before limit slicing."
  artifacts:
    - path: "packages/uk-data/src/uk_data/adapters/ons.py"
      provides: "ONS adapter date-window behavior"
      contains: "start_date"
    - path: "packages/uk-data/src/uk_data/adapters/boe.py"
      provides: "BoE adapter date-window behavior"
      contains: "start_date"
    - path: "packages/uk-data/src/uk_data/adapters/land_registry.py"
      provides: "Land Registry date-window behavior"
      contains: "start_date"
  key_links:
    - from: "packages/uk-data/src/uk_data/adapters/ons.py"
      to: "packages/uk-data/src/uk_data/utils/timeseries.py"
      via: "date-window helper invocation"
      pattern: "filter_observations_by_date_window"
    - from: "packages/uk-data/src/uk_data/adapters/boe.py"
      to: "packages/uk-data/src/uk_data/utils/timeseries.py"
      via: "date-window helper invocation"
      pattern: "filter_observations_by_date_window"
---

<objective>
Apply canonical date-window semantics across core time-series adapters.

Purpose: Deliver TS-01/TS-02 in production fetch paths (ONS/BoE/Land Registry) with consistent inclusive behavior and deterministic ordering with `limit`.
Output: Adapter implementations and adapter test suites updated for explicit `start_date`/`end_date` handling.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-date-bounded-series/03-RESEARCH.md
@.planning/phases/03-date-bounded-series/03-01-SUMMARY.md
@packages/uk-data/src/uk_data/adapters/ons.py
@packages/uk-data/src/uk_data/adapters/boe.py
@packages/uk-data/src/uk_data/adapters/land_registry.py

<interfaces>
From `packages/uk-data/src/uk_data/adapters/ons.py`:

```python
def fetch_series(self, series_id: str, **kwargs: object):
    concept = str(kwargs.get("concept", entry.concept))
    limit = int(kwargs.get("limit", 20))
```

From `packages/uk-data/src/uk_data/adapters/boe.py`:

```python
def fetch_series(self, series_id: str, **kwargs: object):
    concept = str(kwargs.get("concept", series_id.lower()))
    limit = int(kwargs.get("limit", 20))
```

From `packages/uk-data/src/uk_data/adapters/land_registry.py`:

```python
def fetch_uk_hpi_history(filepath: str | Path, *, area_name: str = "United Kingdom", limit: int = 20)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement date-window support in ONS and BoE adapters</name>
  <files>packages/uk-data/src/uk_data/adapters/ons.py, packages/uk-data/src/uk_data/adapters/boe.py, packages/uk-data/tests/adapters/test_ons.py, packages/uk-data/tests/adapters/test_boe.py</files>
  <read_first>packages/uk-data/src/uk_data/adapters/ons.py, packages/uk-data/src/uk_data/adapters/boe.py, packages/uk-data/src/uk_data/utils/timeseries.py, packages/uk-data/tests/adapters/test_ons.py, packages/uk-data/tests/adapters/test_boe.py</read_first>
  <behavior>
    - Test 1: ONS `fetch_series` with `start_date`/`end_date` returns only inclusive-bound observations.
    - Test 2: BoE `IUMABEDR` path filters mocked historical rows by date window before applying `limit`.
    - Test 3: Existing non-windowed adapter tests continue passing.
  </behavior>
  <action>In both adapters, read `start_date`/`end_date` from `kwargs`, apply shared `filter_observations_by_date_window` helper to raw observation rows before constructing canonical `TimeSeries`, then apply existing `limit` behavior on the filtered set (per TS-03 migration semantics from research). Preserve existing fallback behavior for point-series paths while accepting window kwargs without failure.</action>
  <acceptance_criteria>- `ons.py` and `boe.py` reference `start_date` and `end_date` in fetch flow.
- `test_ons.py` and `test_boe.py` include explicit date-window assertions.
- Adapter tests pass with no regressions in previously covered behavior.</acceptance_criteria>
  <verify>
    <automated>uv run pytest packages/uk-data/tests/adapters/test_ons.py packages/uk-data/tests/adapters/test_boe.py -q</automated>
  </verify>
  <done>ONS and BoE adapters enforce shared date-window semantics for time-series responses.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement date-window support in Land Registry time-series path</name>
  <files>packages/uk-data/src/uk_data/adapters/land_registry.py, packages/uk-data/tests/adapters/test_land_registry.py</files>
  <read_first>packages/uk-data/src/uk_data/adapters/land_registry.py, packages/uk-data/src/uk_data/utils/timeseries.py, packages/uk-data/tests/adapters/test_land_registry.py</read_first>
  <behavior>
    - Test 1: `fetch_uk_hpi_history` with `start_date`/`end_date` keeps only rows inside inclusive bounds.
    - Test 2: When both window and `limit` are provided, filtering occurs first, then tail limit is applied.
  </behavior>
  <action>Extend `fetch_uk_hpi_history` and `LandRegistryAdapter.fetch_series("uk_hpi_full", ...)` to accept `start_date` and `end_date`, filter area-sorted observations with shared helper semantics, and preserve existing `limit` tail behavior on the post-filter result. Keep `uk_hpi_average` point-series path intact.</action>
  <acceptance_criteria>- `land_registry.py` supports `start_date`/`end_date` for `uk_hpi_full`.
- `test_land_registry.py` contains coverage for inclusive filtering and filter-then-limit ordering.
- Existing fixture-based tests remain green.</acceptance_criteria>
  <verify>
    <automated>uv run pytest packages/uk-data/tests/adapters/test_land_registry.py -q</automated>
  </verify>
  <done>Land Registry monthly series path now conforms to canonical date-window contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Resolver kwargs → adapter fetch methods | Untrusted boundary inputs consumed in source adapters |
| External API payload dates → canonical filtering | Upstream date labels can be malformed or surprising |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | T (Tampering) | Adapter date filter inputs | mitigate | Route all `start_date`/`end_date` through shared parser/filter utility with strict error handling |
| T-03-05 | I (Information Disclosure) | Adapter observation slicing | mitigate | Enforce filter-before-limit ordering to prevent out-of-window records leaking through tail slices |
| T-03-06 | D (Denial of Service) | Large historical payload processing | accept | Current adapter fetch and limit mechanics already constrain response sizes; no additional remote fan-out added |
</threat_model>

<verification>
- `uv run pytest packages/uk-data/tests/adapters/test_ons.py packages/uk-data/tests/adapters/test_boe.py packages/uk-data/tests/adapters/test_land_registry.py -q`
- `uv run ruff check packages/uk-data/src/uk_data/adapters/ons.py packages/uk-data/src/uk_data/adapters/boe.py packages/uk-data/src/uk_data/adapters/land_registry.py`
</verification>

<success_criteria>
- Core adapters (ONS, BoE, Land Registry) all accept and enforce inclusive `start_date`/`end_date`.
- Date filtering is consistent via shared utility usage.
- Filter-then-limit ordering is test-covered.
</success_criteria>

<output>
After completion, create `.planning/phases/03-date-bounded-series/03-02-SUMMARY.md`
</output>
