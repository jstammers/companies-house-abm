---
phase: 03.1-ons-dataset-api-migration-use-dataset-edition-version-dimens
plan: 02
type: execute
wave: 2
depends_on: [03.1-01]
files_modified:
  - packages/uk-data/src/uk_data/adapters/ons.py
  - packages/uk-data/src/uk_data/adapters/ons_models.py
  - packages/uk-data/tests/adapters/test_ons.py
  - packages/uk-data/tests/test_client.py
  - packages/uk-data/tests/adapters/test_ons_integration.py
autonomous: true
requirements: [TBD]
must_haves:
  truths:
    - "ONSAdapter.get_series is derived from dataset-first methods, not manifest/provider indirection."
    - "Date-window and limit semantics from Phase 03 remain unchanged after the method-surface rewrite."
    - "Fallback series continue to resolve via point-series handlers without regression."
  artifacts:
    - path: "packages/uk-data/src/uk_data/adapters/ons.py"
      provides: "Series derivation layer from dataset API methods to canonical TimeSeries"
      contains: "class ONSAdapter"
    - path: "packages/uk-data/src/uk_data/adapters/ons_models.py"
      provides: "Typed dataset/version/observation models used by ONSAdapter"
    - path: "packages/uk-data/tests/adapters/test_ons.py"
      provides: "Offline ONS adapter migration regressions"
    - path: "packages/uk-data/tests/test_client.py"
      provides: "Client-level passthrough and compatibility checks for ONS series fetch behavior"
    - path: "packages/uk-data/tests/adapters/test_ons_integration.py"
      provides: "Live endpoint compatibility checks for advertised series"
  key_links:
    - from: "packages/uk-data/src/uk_data/adapters/ons.py"
      to: "packages/uk-data/src/uk_data/adapters/ons_models.py"
      via: "get_observation/get_observation_series model usage"
      pattern: "ONSObservation|ONSDatasetVersionInfo"
    - from: "packages/uk-data/src/uk_data/adapters/ons.py"
      to: "packages/uk-data/src/uk_data/utils/timeseries.py"
      via: "filter_observations_by_date_window"
      pattern: "filter_observations_by_date_window"
---

<objective>
Derive `get_series` behavior from the new dataset-first ONSAdapter methods and lock compatibility semantics.

Purpose: Ensure protocol-facing series retrieval uses the new method surface while preserving existing caller behavior.
Output: Rewired `fetch_series`/`get_series` internals + regression coverage at adapter/client boundaries.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-date-bounded-series/03-01-SUMMARY.md
@.planning/phases/03-date-bounded-series/03-02-SUMMARY.md
@.planning/phases/03.1-ons-dataset-api-migration-use-dataset-edition-version-dimens/03.1-RESEARCH.md
@.planning/phases/03.1-ons-dataset-api-migration-use-dataset-edition-version-dimens/03.1-01-PLAN.md
@packages/uk-data/src/uk_data/adapters/ons.py
@packages/uk-data/src/uk_data/adapters/ons_models.py
@packages/uk-data/tests/adapters/test_ons.py
@packages/uk-data/tests/test_client.py
@packages/uk-data/tests/adapters/test_ons_integration.py

<interfaces>
From `packages/uk-data/src/uk_data/adapters/ons.py`:

```python
class ONSAdapter:
    def available_series(self) -> list[str]: ...
    def list_datasets(self, ...) -> list[ONSDatasetInfo]: ...
    def get_dataset(self, dataset_id: str) -> ONSDatasetInfo: ...
    def get_version(self, dataset_id: str, edition: str, version: str | int) -> ONSDatasetVersionInfo: ...
    def get_observation(self, dataset_id: str, edition: str, version: str | int, **dimensions: str) -> ONSObservation: ...
    def get_observation_series(self, dataset_id: str, edition: str, version: str | int, **dimensions: str) -> list[ONSObservation]: ...
    def fetch_series(self, series_id: str, **kwargs: object): ...
```

From `packages/uk-data/src/uk_data/utils/timeseries.py`:

```python
def filter_observations_by_date_window(
    observations: list[dict[str, object]],
    *,
    start_date: str | date | datetime | None = None,
    end_date: str | date | datetime | None = None,
) -> list[dict[str, object]]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewire get_series/fetch_series to derive from dataset-first methods</name>
  <files>packages/uk-data/src/uk_data/adapters/ons.py, packages/uk-data/tests/adapters/test_ons.py, packages/uk-data/tests/test_client.py</files>
  <read_first>packages/uk-data/src/uk_data/adapters/ons.py, packages/uk-data/tests/adapters/test_ons.py, .planning/phases/03-date-bounded-series/03-02-SUMMARY.md</read_first>
  <behavior>
    - Test 1: ONS series retrieval now uses `get_observation`/`get_observation_series` internally.
    - Test 2: Inclusive date-window + filter-before-limit behavior remains unchanged.
    - Test 3: `HP7A` and `D7RA` still route through fallback point-series handlers.
    - Test 4: UKDataClient `get_series(...source="ons")` behavior remains API-compatible.
  </behavior>
  <action>Refactor ONS adapter series derivation to call the new dataset methods and map resulting observations to canonical TimeSeries. Keep `fetch_series` signature stable, and if a `get_series` alias exists/gets introduced, ensure both routes share one implementation. Preserve time-window/limit ordering from Phase 03. Update adapter and client tests to assert method-surface-derived behavior rather than manifest/provider implementation details.</action>
  <acceptance_criteria>
    - `packages/uk-data/src/uk_data/adapters/ons.py` derives all non-fallback series from dataset-first methods.
    - Adapter tests cover window-first-then-limit semantics and fallback handlers.
    - Client tests confirm ons source still resolves correctly through UKDataClient.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest packages/uk-data/tests/adapters/test_ons.py -q</automated>
    <automated>uv run pytest packages/uk-data/tests/test_client.py -q</automated>
  </verify>
  <done>ONS protocol-facing series behavior is preserved while implementation is fully derived from the new method surface.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock live compatibility and remove references to deleted modules</name>
  <files>packages/uk-data/tests/adapters/test_ons_integration.py</files>
  <read_first>packages/uk-data/tests/adapters/test_ons_integration.py, packages/uk-data/src/uk_data/adapters/ons.py</read_first>
  <behavior>
    - Test 1: Integration loop over `available_series()` still succeeds/skips deterministically under network gating.
    - Test 2: Live fetches validate source tagging and non-null latest values for manifest-backed series.
    - Test 3: No test/import path references removed `ons_manifest`/`ons_provider` modules.
  </behavior>
  <action>Update integration tests to remain adapter-bound and dataset-endpoint agnostic while preserving network-gated behavior. Remove stale imports/references to deleted modules from tests and adapter package internals. Keep assertions focused on external behavior (data availability, source tags, shape).</action>
  <acceptance_criteria>
    - Integration test module continues to gate on endpoint reachability before live calls.
    - Integration tests iterate through `adapter.available_series()` and validate non-null results/source tagging.
    - Codebase has no runtime/test references to removed `ons_manifest`/`ons_provider` modules.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest packages/uk-data/tests/adapters/test_ons_integration.py -m integration -q</automated>
    <automated>uv run ruff check packages/uk-data/src/uk_data/adapters/ons.py packages/uk-data/tests/adapters/test_ons.py packages/uk-data/tests/adapters/test_ons_integration.py packages/uk-data/tests/test_client.py</automated>
  </verify>
  <done>Live compatibility checks remain intact and migration cleanup removes obsolete module coupling.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Adapter → Provider | Adapter relies on provider-normalized external observations |
| Integration tests → Live ONS API | Network response availability and schema are untrusted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.1-05 | T | `ONSAdapter.fetch_series` derivation branch | mitigate | Restrict non-fallback series path to dataset-method results and raise explicit errors for unsupported series ids. |
| T-03.1-06 | I | Date-window filtering in adapter | mitigate | Preserve shared utility filtering and lock with regression tests for inclusive bounds + ordering. |
| T-03.1-07 | D | Live integration endpoint volatility | accept | Keep network-gated integration tests with skip-on-unreachable behavior. |
| T-03.1-08 | R | Migration ambiguity in code paths | mitigate | Remove manifest/provider dependencies and assert method-surface-derived behavior in adapter/client tests. |
</threat_model>

<verification>
- `uv run pytest packages/uk-data/tests/adapters/test_ons.py -q`
- `uv run pytest packages/uk-data/tests/test_client.py -q`
- `uv run pytest packages/uk-data/tests/adapters/test_ons_integration.py -m integration -q`
</verification>

<success_criteria>
- ONS adapter series derivation is fully migrated to dataset-first methods on ONSAdapter.
- Date-window semantics from Phase 03 remain intact.
- Offline/client/integration tests all protect migrated behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-ons-dataset-api-migration-use-dataset-edition-version-dimens/03.1-02-SUMMARY.md`
</output>
